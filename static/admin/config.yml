backend:
  name: git-gateway
  branch: main
media_folder: "static/uploads"
public_folder: "uploads"
collections:
  - name: "teasers"
    label: "Artikelvorschauen"
    folder: "main-page/teasers"
    create: true
    fields:
      - { label: Titel, name: title, widget: "string" }
